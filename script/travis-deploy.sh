#!/usr/bin/env bash

# This is a silly workaround to allow us to use secure values for our deployments without losing the ability to do
export RS_USERNAME=$1
export RS_API_KEY=$2
export RS_REGION_NAME=$3